[General]
# The default localAddress of 0.0.0.0 makes the server reachable at all of its local addresses
# You almost never have to change this
localAddress = 0.0.0.0
port = 25565
maximumPlayers = 100
hostname = ArenaMP server
# 0 - Verbose (spam), 1 - Info, 2 - Warnings, 3 - Errors, 4 - Only fatal errors
logLevel = 2
password =

[Plugins]
home = ./server
plugins = serverCore.lua

[MasterServer]
enabled = false
address = master.tes3mp.com
port = 25561
rate = 10000

[Voice]
# Realtime proximity voice is never persisted.
enabled = true
# Authoritative server-side routing radius in metres.
rangeMeters = 30
